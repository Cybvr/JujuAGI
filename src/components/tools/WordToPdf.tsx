import React from 'react';

const WordToPdf: React.FC = () => {
  return (
    <div>
      <h2>Convert Word to PDF Tool</h2>
      {/* Add your tool implementation or iframe here */}
      <p>This tool will convert Word documents to PDF format.</p>
    </div>
  );
};

export default WordToPdf;